// Jest 설정 파일
// 테스트 실행 전 공통 설정을 여기에 추가합니다

// 타임아웃 설정
jest.setTimeout(10000);

// 전역 모킹 설정
global.console = {
  ...console,
  // 테스트 중 불필요한 로그 출력 방지
  log: jest.fn(),
  debug: jest.fn(),
  info: jest.fn(),
  warn: jest.fn(),
  error: jest.fn(),
};