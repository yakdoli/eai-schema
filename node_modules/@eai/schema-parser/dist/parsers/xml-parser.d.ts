import { XMLParseResult, ParseOptions } from '../types';
/**
 * XML 파싱 엔진
 * fast-xml-parser를 사용하여 XML 문서를 파싱하고 네임스페이스를 처리합니다.
 */
export declare class XMLParser {
    private fastParser;
    private namespaceResolver;
    private errorHandler;
    constructor();
    /**
     * XML 문자열을 파싱합니다.
     * @param xmlContent XML 문자열
     * @param options 파싱 옵션
     * @returns 파싱 결과
     */
    parse(xmlContent: string, options?: ParseOptions): Promise<XMLParseResult>;
    /**
     * 입력 검증
     */
    private validateInput;
    /**
     * XXE 공격 패턴 검사
     */
    private containsXXEPatterns;
    /**
     * XML 구문 검증 (xmldom 사용)
     */
    private validateSyntax;
    /**
     * 파싱된 객체를 XML 문자열로 변환
     */
    toXML(data: any, options?: {
        format?: boolean;
    }): string;
}
//# sourceMappingURL=xml-parser.d.ts.map